// Basic test for Travelly. The full app needs Hive initialised, so this is a
// lightweight placeholder; add widget/integration tests as the app stabilises.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('sanity', () {
    expect(1 + 1, equals(2));
  });
}
